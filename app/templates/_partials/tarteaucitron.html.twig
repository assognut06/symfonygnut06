<link href="https://cdn.jsdelivr.net/npm/tarteaucitronjs@1.21.0/css/tarteaucitron.min.css" rel="stylesheet">
	 <script src="https://cdn.jsdelivr.net/npm/tarteaucitronjs@1.21.0/tarteaucitron.min.js"></script>
		 <script>
		  function initTarteaucitron() {
		    if (typeof tarteaucitron !== 'undefined') {
		      tarteaucitron.init({
		        privacyUrl: '/mention/legales',
				hashtag: '#tarteaucitron', // Outil de gestion des cookies
				cookieName: 'tarteaucitron',
		        orientation: 'popup', // "popup", "middle", "bottom", "top"
				showAlertSmall: false,
		        cookieslist: true,
		        highPrivacy: true, // Activer le mode haute confidentialité
		        iconPosition: 'BottomLeft'
				{# googleConsentMode:true, // Activer le mode de consentement Google #}
				{# bingConsentMode:true, #}
		      });
		      // Déclaration des services
		      tarteaucitron.user.gtagUa = 'G-0BTCZT3QCS'; // Ton ID GA4
		      tarteaucitron.job = tarteaucitron.job || [];
		      tarteaucitron.job.push('gtag');
		
		      tarteaucitron.user.awConversionId = 'AW-16920455195'; // Ton ID Google Ads
		      tarteaucitron.job.push('googleadwordsconversion');
		    } else {
		      setTimeout(initTarteaucitron, 100);
		    }
		  }
		  initTarteaucitron();
		</script>