<form method="post" action="{{ path('app_admin_payers_delete', {'id': payer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payer.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
