<form method="post" action="{{ path('app_assos_crud_delete', {'id': asso_recommander.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asso_recommander.id) }}">
    <button class="btn">Delete</button>
</form>
